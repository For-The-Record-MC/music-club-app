# Database schema

**Placeholder — the database is empty in Phase 0.** The first migration (profiles, clubs, club_members, invite RPCs, RLS) lands in Phase 1; this file gets the real table-by-table reference then.

Until then, the planned schema sketch and its invariants (cycle lifecycle, the three-stage rating-visibility ladder, the wheel-eligibility rule, role model) live in [../PLAN.md](../PLAN.md).
